{
  "name": "spec",
  "version": "0.0.0",
  "description": "demo spec",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "tslint -p .",
    "format": "prettier --write \"**/*.ts\"",
    "postformat": "tslint -p . --fix",
    "prebuild": "./gen-index && npm run format",
    "build": "tsc",
    "postbuild": "./rm-index"
  },
  "keywords": [],
  "author": "Beeno Tung",
  "license": "BSD-2-Clause",
  "devDependencies": {
    "idl2ts": "^0.3.3",
    "prettier": "^1.13.7",
    "tslint": "^5.9.1",
    "tslint-eslint-rules": "^5.3.1",
    "typescript": "^2.9.2"
  }
}
